SSU 1.4.0-dev1 validation report
Date: 2026-07-29
Baseline: SSU 1.3.0-dev4.3 source
Target: Minecraft 26.2 / NeoForge 26.2.0.7-beta / Java 25
Network protocol: 13

RESULT SUMMARY
- Combined dashboard phase 4 and Core 2.0 phase 5 source implementation: COMPLETED
- Compact shell, bounded page payloads and typed actions: IMPLEMENTED
- Claims/Permissions/Regions/Teleport/Visualization/Menu module ownership: IMPLEMENTED
- Source-level, payload and lifecycle regression checks: PASS
- Isolated compilation of modified dashboard/menu/module code with API stubs: PASS
- JSON and PNG asset validation: PASS
- Confirmed dev4.3 map renderer/cache unchanged: PASS
- Full Gradle/NeoForge compile: NOT COMPLETED IN THIS ENVIRONMENT

FULL BUILD LIMITATION
The Gradle wrapper attempted to download Gradle 9.2.1, but this environment
could not resolve services.gradle.org:

  java.net.UnknownHostException: services.gradle.org

The installed JVM is OpenJDK 21.0.10 while the project toolchain requires Java
25. No claim is made that a distributable NeoForge JAR was compiled here. Build
locally with Java 25 using:

  gradlew.bat clean test build

CHECKS COMPLETED
1. Java lexical delimiter scan
   PASS: 173 Java source/test files scanned while ignoring comments, strings,
   character literals and text blocks. No unbalanced braces, parentheses or
   brackets were found.

2. Dashboard payload regression harness
   PASS: page identifiers and searches normalize correctly.
   PASS: negative page/request values are clamped safely.
   PASS: requested page size is bounded to 50 entries.
   PASS: action identifiers and fields are normalized and bounded.
   PASS: oversized page response lists are rejected before encoding.
   PASS: transaction reason/failure strings are bounded before encoding.
   PASS: 4 payload tests completed successfully.

3. Core module registry regression harness
   PASS: dependencies initialize/start before dependants.
   PASS: shutdown runs in reverse dependency order.
   PASS: missing dependencies are rejected.
   PASS: cyclic dependencies are rejected.
   PASS: 3 module-registry tests completed successfully.

4. Isolated Java compilation with API stubs
   PASS: SsuDashboardScreen and all new dashboard payload interfaces compiled
   with minimal Minecraft/NeoForge signature stubs.
   PASS: SsuMenuService and its page/action payload wiring compiled with
   controlled manager/API signature stubs.
   PASS: ClaimModule, PermissionModule, RegionModule, TeleportModule,
   VisualizationModule and MenuModule compiled with lifecycle/service stubs.
   PASS: module order harness completed successfully.

5. Dashboard/UI invariants
   PASS: normal layouts retain texture-backed module tiles, custom settings and
   admin controls, custom back texture and the framed player portrait.
   PASS: narrow layouts avoid the old overlapping bottom-left back control by
   moving Back to the header; the home page uses the close control.
   PASS: stale page responses and stale action results are rejected.
   PASS: page/search/draft state is retained when the compact shell refreshes.
   PASS: region unrent requires a second confirmation click.

6. Network and server-authority invariants
   PASS: protocol registrar is version 13.
   PASS: opening the dashboard sends an empty compatibility list shell rather
   than all claims, regions, homes and warps.
   PASS: all new request/response lists and strings are bounded.
   PASS: dashboard mutations use a closed action switch; no arbitrary command
   string is accepted by the new dashboard channel.
   PASS: payment, economy administration, permission administration, region
   visibility/renting, teleport, job cancellation and core reset paths each
   revalidate their permissions and targets on the server.

7. Core 2.0 lifecycle invariants
   PASS: Claims, Permissions, Regions/Snapshots/Rent Journal, Teleport,
   Visualization and Menu register through SsuModule implementations.
   PASS: existing managers remain the authoritative compatibility facade.
   PASS: active jobs are cancelled before reverse-order module shutdown saves.
   PASS: session-only pending teleports/cooldowns are cleared on shutdown.
   PASS: Homes, Warps, UI preferences, storage, jobs, performance and
   transactions remain registered through the legacy boundary for a later
   incremental migration.

8. Data/map compatibility invariants
   PASS: no claim, region, snapshot, rent, economy, permission, home, warp or
   UI-preference save codec/schema was changed.
   PASS: region snapshot format remains version 3.
   PASS: no production file under the dev4.3 aerial map/minimap renderer was
   modified; renderer fingerprint and persistent tile format remain unchanged.
   PASS: installing this source should not force a dev4.3 map-cache rebuild.

9. JSON, PNG and whitespace validation
   PASS: 13 JSON files parsed successfully.
   PASS: 13 PNG files have valid signatures and chunk CRCs.
   PASS: git diff --check reports no whitespace errors when preserving the
   existing CRLF TeleportManager file.

RUNTIME TESTS STILL REQUIRED
- Full Java 25 Gradle test/build against the real NeoForge 26.2 APIs.
- Startup of a copied dev4.3 world without duplicate service or module errors.
- Matching protocol-13 client/server connection and protocol-12 rejection.
- Dashboard rendering at normal and narrow GUI scales.
- Page search, pagination, stale response handling and draft preservation.
- Every typed player/admin action with allowed and denied permissions.
- Module startup/shutdown and active-job cancellation in a real server.
- Persistence checks for all existing world/player data after restart.
- Confirmation that the accepted dev4.3 minimap/world-map/claim-map appearance
  and persistent cache return without a rebuild.
