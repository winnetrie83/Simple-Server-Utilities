SSU 1.6.0-dev12.1 validation report

Baseline
- Parent source: SSU 1.6.0-dev12
- Mod version: 1.6.0-dev12.1
- Network protocol: 35 (unchanged)
- Player UI preferences: schema 6 (unchanged)
- Personal map markers: schema 1 (unchanged)
- Aerial map cache: format 4 / map-cache-v3 (unchanged)

Implemented interaction corrections
- World Map right-click calls the marker context action directly on mouse press.
- World Map panning uses button 2 (middle mouse) only.
- Claim Map panning uses button 2 (middle mouse) only.
- Both parent screens forward active middle drag/release events to their map widget.
- Right mouse is no longer part of either drag state.
- Bottom controls use `height - MARGIN - 25`, exactly 5 pixels above dev12.
- Duplicate bottom map-switch controls are absent.
- Tooltips are `Manage markers` and `refresh`.
- Status hints describe middle-button panning.

Static validation
- 296 Java source files scanned.
- Java comments, strings, chars and (), [] and {} delimiters are balanced.
- Whole-source javac 21 parse against the downgraded Minecraft 26.2 merged jar reported no syntax diagnostics. Expected unresolved NeoForge/Gson symbols remain because the complete Gradle dependency graph is unavailable in this container.
- `javap` confirmed the Minecraft 26.2 MouseButtonEvent, AbstractWidget and Screen mouse method signatures used by the changed classes.
- Fourteen targeted source assertions passed for right-click context, middle drag, parent forwarding, labels, positions and removed duplicate controls.

Build limitation
- A complete NeoForge/Gradle build was not run in this container because Java 25 and the cached Gradle 9.2.1 distribution are unavailable. Build locally with Java 25 using `gradlew.bat clean build`.
