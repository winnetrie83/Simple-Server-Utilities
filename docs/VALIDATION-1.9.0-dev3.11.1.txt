SSU 1.9.0-dev3.11.1 validation notes

Compile hotfix on top of dev3.11.

Corrections:
- HologramEditorScreen rich-text palette tooltip lambda now captures an effectively-final paletteIndex.
- MineEvents uses ServerPlayer.sendOverlayMessage(Component) for action-bar permission feedback.
- MineManager uses sendOverlayMessage(Component) for ACTIONBAR reset warnings and sendSystemMessage(Component) for normal warnings.

Version/schema invariants:
- mod_version = 1.9.0-dev3.11.1
- network protocol = 96 (unchanged)
- Server Operations schema = 3 (unchanged)
- Mine definition schema = 1 (unchanged)

The dev3.11 runtime checklist remains applicable: docs/TESTING-1.9.0-dev3.11.md

Checks performed:
- Dependency-less javac parser sanity on the three corrected Java files: no parser/syntax diagnostics; missing dependency/type diagnostics are expected without the Minecraft/NeoForge classpath.
- No remaining `displayClientMessage` calls in `src/main/java`.
- Gradle compile attempt could not reach the compiler because the wrapper needs Gradle 9.2.1 and `services.gradle.org` is unavailable in the container (`UnknownHostException`).
