SSU 1.6.0-dev10.1 validation report
====================================

Baseline
--------
- Parent source: SSU 1.6.0-dev10
- Mod version: 1.6.0-dev10.1
- Network protocol: 31 (unchanged)
- Stored-data schemas: unchanged

Validated changes
-----------------
- Border capability permissions use PermissionService.getBooleanWithoutOperatorBypass.
- Explicit BORDER_REGIONS_VIEW=false cannot be bypassed by an operator.
- Claim border capability uses the same hard-gate semantics for consistency.
- Effective visibility is recomputed and compared with the previous sync state every refresh cycle.
- REGION, REGION_FOCUS and SELECTION clear when the effective region gate is false.
- Region Show actions reject a denied server capability.
- Dashboard capability state uses the same strict permission resolution.

Static validation
-----------------
- 266 Java source files passed lexical/string/comment and delimiter balance scanning.
- Source assertions confirmed no legacy operator-bypassing BORDER_REGIONS_VIEW calls remain in the renderer, dashboard action service or border commands.
- git diff --check passed.
- Source zip integrity test passed.

Build limitation
----------------
A full Gradle/NeoForge compile could not be run in this container. The wrapper attempted to fetch Gradle 9.2.1, but external network access is unavailable; only Java 21 is installed while this project targets Java 25. Build locally with Java 25 using:

    gradlew.bat clean build
