SSU 1.9.0-dev3.4 static validation report
==========================================

Base
----
Built on: SSU 1.9.0-dev3.3.3
Mod version: 1.9.0-dev3.4
Network protocol: 89
Region storage schema: 5
Minigame definition/recovery/progression/history: 19/4/3/1
Player UI preferences: 11
Title catalogue / Player Identity: 1/1

Validation completed
--------------------
PASS - Custom lexical scan of 642 Java source files: zero unclosed strings, comments or delimiters.
PASS - All 15 project JSON files parsed successfully.
PASS - 42/42 targeted source invariants passed.
PASS - RegionSetupOpenPayload, RegionSetupRequestPayload, RegionSetupActionPayload and RegionSetupSavePayload compiled against bounded networking API stubs.
PASS - RegionSetupScreen compiled against GUI/network API stubs.
PASS - RegionSelectionToolScreen compiled against GUI/network API stubs after rerouting legacy creation into Region Setup Tool.
PASS - RegionSetupToolService compiled against server, region, economy, job and networking API stubs.
PASS - RegionResetScheduler compiled against server/job API stubs.
PASS - DamageIndicatorClientState and DamageIndicatorRenderer compiled against client renderer API stubs.
PASS - DamageIndicatorStyle, RegionResetMode and RegionResetSettings compiled with javac; pure behavior harness passed.
PASS - CREATE input is validated before RegionManager#create, preventing partial regions after invalid rent/preset input.
PASS - The source archive contains no generated .class/.pyc files or build directory.

Important source invariants
---------------------------
- Pop no longer appends `!`.
- Burst no longer wraps values in `[]`.
- DROP is selectable after Burst and cycles back to Floating.
- DROP uses a tight entity origin and a short upward pop followed by a downward quadratic path.
- Right-clicking the Region Tool on a block or in air reaches RegionSetupToolService.
- Current-position region lookup opens edit mode; absent region opens selection/create mode.
- General, Protection, Rent & access and Scheduled reset tabs exist.
- All eleven RegionSettings protection flags are exposed.
- Context permission overrides remain accessible.
- Snapshot and weighted-preset reset sources are persisted.
- Preset remainder becomes air and both client/server cap the preset at six entries.
- Scheduler respects wait-until-empty, minigame ownership, resource locks and rental-recovery guards.
- Snapshot capture, Reset now and bounded preset fill all submit through the SSU job system.
- Region redefine copies scheduled-reset settings.
- Region JSON writes schemaVersion 5 and the example template contains scheduledReset.

Full Gradle/NeoForge build
--------------------------
NOT RUN in the container.
The Gradle wrapper attempted to download Gradle 9.2.1 from services.gradle.org, but DNS/network access is unavailable. The container also provides Java 21 while the project requires Java 25.

Required local verification
---------------------------
Run with Java 25:
    gradlew.bat clean build

Then execute TESTING-1.9.0-dev3.4.md on a dedicated server and separate client.
