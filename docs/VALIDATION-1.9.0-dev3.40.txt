SSU 1.9.0-dev3.40 static validation
======================================

Scope
-----
Shared server-wide NPC Ability Library, assignment migration, standalone Ability Workshop,
and smarter stationary/target-count combat eligibility.

Completed checks
----------------
- 873 Java source files scanned with a delimiter-aware Java scanner: 0 unbalanced (), [] or {}.
- All project-local `be.winnetrie.mod.simpleserverutilities...` imports resolve to a source top-level class (nested imports allowed): 0 missing.
- Pure NPC combat data layer compiled with local javac 21:
  NpcAttackKind, NpcAbilityShape, NpcDamageSchool, NpcAbilityType,
  NpcAbilityDefinition, NpcAbilityPreset, NpcAbilityAssignment.
- No stale NpcEditorScreen references to the old per-NPC Ability Workshop/preset flow.
- New reusable ability payloads are registered in ModNetworking protocol 118.
- NPC definition schema is 19; legacy schema <=18 embedded abilities are migrated before normalization.
- Shared ability IDs are protected against accidental overwrite when creating a new global ability.
- Thunderclap preset: Around self, Requires stationary ON, Min targets 1.
- Arcane Missiles preset: Requires stationary ON, channeling, interrupt-on-damage/move.
- Runtime target eligibility counts actual DamageFilter-approved targets in the configured shape.
- Global ability deletion is blocked while an NPC template still assigns the ability.

Not performed in this sandbox
-----------------------------
The full Gradle/NeoForge compile/runtime test cannot be executed here because the Gradle 9.2.1
distribution is not cached, services.gradle.org is unreachable from the sandbox, and the local JDK
is Java 21 while this project targets Java 25. A local Java 25 NeoForge 26.2 build remains required.
