SSU 1.3.0-dev4.3 validation report
Date: 2026-07-29
Baseline: SSU 1.3.0-dev4.2 source
Target: Minecraft 26.2 / NeoForge 26.2.0.7-beta / Java 25
Network protocol: 12

RESULT SUMMARY
- Clean-room comparison of the supplied JourneyMap JAR: COMPLETED
- Source-level and pure-Java regression checks: PASS
- Aerial storage v3 round-trip/coalescing harness: PASS
- Isolated compilation of modified atlas/storage classes with API stubs: PASS
- JSON and PNG asset validation: PASS
- Patch reproducibility: PASS before this report was added; repeated for final package
- Full Gradle/NeoForge compile: NOT COMPLETED IN THIS ENVIRONMENT

CLEAN-ROOM COMPARISON
The user supplied JourneyMap 6.0.2 for NeoForge 26.2, SHA-256:

  b8b008c18415d1c51046eb54af8752ee47949c1f4c8860149cdb4a39fc735526

Only class names, public/private metadata and bytecode-observable rendering
concepts were inspected. The relevant observations were that established map
renderers distinguish foliage/plants/water/no-shadow categories and combine
near and secondary north/west slope samples. SSU uses independently designed
classes, calculations, factors and persistence. No JourneyMap class, source,
bytecode fragment, texture, asset, identifier or configuration file is present
in SSU production sources.

FULL BUILD LIMITATION
The Gradle wrapper attempted to download Gradle 9.2.1, but this environment
could not resolve services.gradle.org:

  java.net.UnknownHostException: services.gradle.org

The installed JVM is OpenJDK 21.0.10 while the project toolchain requires Java
25. No claim is made that a distributable NeoForge JAR was compiled here. Build
locally with Java 25 using:

  gradlew.bat clean test build

CHECKS COMPLETED
1. Java lexical delimiter scan
   PASS: 161 Java source/test files scanned while ignoring comments, strings,
   character literals and text blocks. No unbalanced braces, parentheses or
   brackets were found.

2. Pure-Java TerrainReliefMath harness
   PASS: flat local, terrace, broad and macro passes return exactly 1.0.
   PASS: north-west macro slope = 1.094350.
   PASS: opposing south-east macro slope = 0.876850.
   PASS: rising two-distance terrace = 1.054680.
   PASS: descending two-distance terrace = 0.923400.
   PASS: north-west-exposed canopy edge = 1.036320.
   PASS: south-east-exposed canopy edge = 0.902475.
   PASS: non-canopy and extreme canopy neighbour heights are bounded.

3. Aerial map storage v3 harness
   PASS: two writes to one tile retain the latest pixel generation.
   PASS: terrain heights survive gzip/atomic disk round-trip.
   PASS: mapped-surface heights survive disk round-trip.
   PASS: solid/canopy/water surface-kind bytes survive disk round-trip.
   PASS: asynchronous flush and subsequent read completed within five seconds.

4. Isolated Java compilation with API stubs
   PASS: AerialMapStorage and AerialMapCacheCoordinates compiled and executed
   with minimal Minecraft/SSU stubs.
   PASS: AerialMapAtlas compiled with minimal API-signature stubs, validating
   the new constructor, storage and height-grid wiring at Java type level.

5. JSON and PNG validation
   PASS: 13 JSON files parsed.
   PASS: 13 PNG files have valid signatures and chunk CRCs.

6. Source invariants
   PASS: mod_version is 1.3.0-dev4.3.
   PASS: networking registrar remains version 12.
   PASS: renderer fingerprint is atlas-topographic-v4.
   PASS: aerial cache tile format is version 3.
   PASS: region snapshot format remains version 3 and is unchanged.
   PASS: every captured tile carries terrain heights, visible-surface heights
   and surface-kind metadata.
   PASS: decorative vegetation filtering remains present.
   PASS: canopies use independent dark colour grading and local height relief.
   PASS: broad/macro terrain lighting uses the underlying ground height.
   PASS: the dev4.2 blockReliefLight/detailEdgeLight noise passes are absent.
   PASS: missing neighbour metadata is sampled with getChunk(..., false), so
   the renderer does not force-load chunks.

7. Whitespace and patch checks
   PASS: git diff --check produced no whitespace errors.
   PASS: the dev4.2-to-dev4.3 patch applies cleanly to the supplied dev4.2 tree
   and the applied result matches the target source tree.

RUNTIME TESTS STILL REQUIRED
- Visual confirmation that isolated and dense tree crowns are dark, coherent
  and three-dimensional instead of pale spots.
- Visual confirmation that hills remain readable below forest canopies.
- Comparison of plains, terraced hills and mountains at several zoom levels.
- One-time rebuild and later disk reload under atlas-topographic-v4.
- Client capture performance with the larger eight-block relief margin.
- Vanilla and modded foliage classification in actual Minecraft.
- Existing minimap claim-outline, right-drag and drop-free region-reset checks.
