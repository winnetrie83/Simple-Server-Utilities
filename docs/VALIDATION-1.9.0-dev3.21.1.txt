SSU 1.9.0-dev3.21.1 compile-hotfix validation
===============================================

Target version: 1.9.0-dev3.21.1
Network protocol: 103 (unchanged)

User compile report: 12 compile errors, reduced to two root causes.
- Stale nested-type imports from HologramRichTextDocument after the SsuRichTextDocument refactor.
- ChatFormatting.isColor(), unavailable in the Minecraft 26.2 compile API.

Hotfix checks:
- no references remain to HologramRichTextDocument.Format, .Segment or .CharacterStyle;
- no ChatFormatting.isColor / formatting.isColor calls remain;
- no gameplay/protocol/schema/persistence changes were introduced by this hotfix.

Run `gradlew.bat compileJava > compile-report.txt 2>&1` locally to confirm the full Minecraft/NeoForge classpath compile.
