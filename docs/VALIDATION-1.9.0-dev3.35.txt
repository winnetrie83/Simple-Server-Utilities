SSU 1.9.0-dev3.35 validation notes

Target base: 1.9.0-dev3.34.4
Protocol: 115 (114 -> 115)
NPC definition schema: 16 (15 -> 16)
NPC placement schema: 4 (unchanged)

Static validation scope:
- Fixed role enum is no longer used by NPC editor/runtime behavior; NpcRole remains only as a schema<16 migration helper.
- roleId is bounded free-form text (64 chars); roleColor is bounded to classic Minecraft palette indices 0..15.
- NPC editor Identity uses RichTextPalette's existing 16 swatches in a compact 2x8 layout.
- Role color is synchronized in overhead labels and generated NPC function/service menus.
- Shop controls no longer depend on a Merchant role.
- NpcAiProfile derives a species family from the actual runtime locomotion shell and tunes route/combat cadence, route arrival tolerance and wander dimensions.
- Native PathNavigation/MoveControl remains authoritative for physical walking/hopping/flying/swimming.
- Schedule, patrol and wander share the family-aware route completion envelope.
- Combat interruption preserves logical patrol/schedule state and clears only the old movement route before resuming ambient AI.
- Behavior editor payload carries bounded AI family/runtime diagnostic strings.
- Payload constructor arity and modified Java delimiter structure are checked by local static scripts before packaging.

A full Gradle/NeoForge compile cannot be authoritative in this sandbox because the available local JDK is Java 21 while SSU targets Java 25, and the Gradle distribution endpoint may not be reachable. The user must perform the authoritative Java 25 / NeoForge 26.2 compile and runtime test.

Local static-check results before packaging:
- Java source files scanned: 841; delimiter/structure problems: 0.
- simpleserverutilities.mixins.json parses successfully.
- NpcEditorOpenPayload record: 113 components; 3 constructor call sites; 0 arity mismatches.
- NpcEditorSubmitPayload record: 106 components; 2 constructor call sites; 0 arity mismatches.
- NpcFunctionMenuPayload record: 5 components; 2 constructor call sites; 0 arity mismatches.
- Targeted checks confirmed protocol 115, NPC schema 16, 16 role swatches, role color bounds, AI-family routing helpers and no runtime/editor NpcRole dependency outside the legacy migration helper.
- `./gradlew compileJava --offline --no-daemon` could not reach compilation because the wrapper attempted to download Gradle 9.2.1 from services.gradle.org and DNS/network access is blocked in this sandbox. Local Java is 21.0.11 while the project toolchain targets Java 25.
