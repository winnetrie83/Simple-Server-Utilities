SSU 1.9.0-dev3.34.1 validation notes

Scope
- Player NPC movement-speed cleanup
- Native PathNavigation repath smoothing
- Patrol zero-pause flow
- no-AI gravity / native MoveControl conflict guard

Static validation
- 839 Java source files scanned for balanced (), [] and {} delimiters: PASS
- All src/main/resources JSON parsed successfully: PASS
- gradle.properties mod_version: 1.9.0-dev3.34.1
- Network protocol: 114 (unchanged)
- NPC definition schema: 15 (unchanged)
- Player NPC native base movement attribute: 0.25
- Player visual normalization clears legacy movementSpeed override to -1
- Player Stats screen hides the duplicate movement-speed field
- Behavior route speed still accepts 0.05..4.0 and decimal parsing supports dot/comma input
- Behavior path refresh hint raised from 4 to 12 ticks; controller additionally avoids repathing unchanged active paths

Build limitation in ChatGPT sandbox
- Gradle compile could not start because the wrapper distribution URL services.gradle.org is not reachable in this sandbox.
- The sandbox Java runtime is Java 21 while this SSU project targets Java 25.
- User-side Java 25 / NeoForge 26.2 compilation and Minecraft runtime testing remain required.
