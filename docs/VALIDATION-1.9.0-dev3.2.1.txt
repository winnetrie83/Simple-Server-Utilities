SSU 1.9.0-dev3.2.1 validation report
======================================

Base: SSU 1.9.0-dev3.2
Target version: 1.9.0-dev3.2.1
Network protocol: 87
Minigame definition schema: 18
Minigame recovery schema: 4
Minigame progression schema: 3
Match-history schema: 1
Player UI preference schema: 10

Compile-hotfix scope
--------------------
Dev3.2 accidentally removed an established MinigameManager API block together with the obsolete
Ready-check method. This left unresolved calls in TeleportPolicy, MinigameEvents, MinigameCommands
and MinigameManager itself.

Restored methods
----------------
- matchView(UUID)
- finishMatch(UUID, Set<Integer>, String)
- addScore(UUID, long)
- setScore(UUID, long)
- eliminate(UUID, String)
- onPlayerDeath(ServerPlayer)
- onPlayerRespawn(ServerPlayer)
- onLogin(ServerPlayer)

Completed checks
----------------
- Every restored method is present exactly once: PASS.
- Every unresolved call shown in the four affected source files now has its matching public manager method: PASS.
- Obsolete readyCheckSatisfied method remains absent: PASS.
- Whole-source Java parser scan: PASS (608 Java files; no syntax diagnostics under the Java 21 parser).
- Version/protocol/schema invariants: PASS.
- Ready/Unready behaviour was not reintroduced: PASS.

Full build limitation
---------------------
The Gradle build could not start in this container because Gradle 9.2.1 is not cached and the
wrapper cannot resolve services.gradle.org. The container also provides Java 21 while the project
targets Java 25. A local `gradlew.bat clean build` remains required.
