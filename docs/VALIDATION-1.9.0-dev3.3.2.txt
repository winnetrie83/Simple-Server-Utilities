SSU 1.9.0-dev3.3.2 validation report
=====================================

Base
----
- Built on SSU 1.9.0-dev3.3.1.
- Mod version updated to 1.9.0-dev3.3.2.
- Network protocol remains 88.
- No storage or definition schema changes.

Implemented changes
-------------------
1. Rank Prefix Editor
   - Replaced the sixteen plain palette buttons with actual colour-filled buttons.
   - Retained readable palette names with automatic black/white contrast text.
   - Palette remains the fixed sixteen-colour Minecraft list.

2. Rank Management layout
   - Reduced the rename field to approximately half its former width.
   - Positioned Title Manager and Refresh directly after that field without overlap.
   - Clarified the rename field through both its hint and a persistent explanatory line.

3. Title Administration
   - Expanded the Title display-name field from 158 to 320 pixels.
   - Reflowed requirement, colour, unlock, action, preview and manual grant controls.

4. Chat formatting
   - Removed the duplicated vanilla player-name wrapper.
   - Sends one complete SSU-formatted chat line: RankPrefix PlayerName: message.
   - Preserves rank prefix colour/style while leaving player name and message unstyled.
   - Inserts exactly one separating space when a saved prefix has no trailing space.

Static validation
-----------------
- Java lexical delimiter/string/comment scan: 633 files, passed.
- JSON parse validation: 15 files, passed.
- Targeted source invariants: 14/14 passed.
- IdentityEvents targeted API-stub compilation: passed.
- RankDisplayEditorScreen targeted GUI-stub compilation: passed.
- Version/protocol/schema consistency checks: passed.

Full Gradle build
-----------------
- Not completed in this container.
- The Gradle wrapper attempted to fetch Gradle 9.2.1 from services.gradle.org, but the
  container has no network access. The available runtime is Java 21 while this project
  requires Java 25.
- Required local command: gradlew.bat clean build

Runtime status
--------------
- Source is statically validated.
- In-game and dedicated-server behaviour still requires local testing.
