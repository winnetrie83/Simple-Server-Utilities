SSU 1.9.0-dev3.34 validation notes
===================================

Scope
-----
Advanced Combat Patterns + Threat/Aggro built on the user's compile-confirmed dev3.33 source.

Static/source checks completed
------------------------------
- Version changed to 1.9.0-dev3.34.
- Network protocol changed 113 -> 114 because both NPC editor payload shapes changed.
- NPC definition schema changed 14 -> 15.
- Schema <15 explicitly migrates with threatEnabled=false and attackPatternEnabled=false plus safe default threat tuning.
- Open/submit record constructor call arity was checked against every constructor call: 110/110 and 105/105 respectively.
- Attack-pattern packet read/write ordering and bounded count (max 24) are symmetrical in source.
- NPC Editor Page enum and navigation label count are both 14; Tactics is handled by both init and render switches.
- New pure attack-pattern data classes compile under the available javac and basic range/health matching semantics were exercised.
- All 839 Java source files passed a lexical balanced-delimiter scan.
- Ability/phase rename and delete paths update/clear attack-pattern references.
- Boss phase transition/reset paths reset attack-pattern state; encounter reset/runtime removal clear threat state.
- Threat remains opt-in and relation ticking includes threat-enabled definitions.
- Confirmed incoming LivingIncomingDamageEvent uses its current amount only after SSU cancellation/authorization checks.
- SSU Self Heal reports actual health restored to the source-attributed healing threat hook.

Local build limitation in this workspace
----------------------------------------
A Gradle compile was attempted. The wrapper cannot resolve services.gradle.org in this execution environment,
so Gradle 9.2.1 is not downloaded and NeoForge compilation never begins. The container exposes only Java 21;
SSU targets Java 25.

Therefore dev3.34 still needs the normal Java 25 / NeoForge 26.2 build on the user's development machine.
After compiling, follow TESTING-1.9.0-dev3.34.md for runtime validation.
