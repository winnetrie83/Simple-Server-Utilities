SSU 1.6.0-dev12.7 validation report
====================================

Baseline
--------
Parent source: SSU 1.6.0-dev12.6
Mod version: 1.6.0-dev12.7
Minecraft: 26.2
NeoForge: 26.2.0.7-beta
Required Java: 25
Network protocol: 35 (unchanged)
Player UI preference schema: 6 (unchanged)
Map marker schema: 1 (unchanged)
Aerial map client-cache format: 5 / map-cache-v4 (unchanged)
Server-persistent data schemas: unchanged

Implemented scope
-----------------
1. Marker readability
   - Filled marker radii reduced from 0.29/0.265 to 0.145/0.1325 blocks.
   - Label text scale increased from 0.16 to 0.36 (2.25x).
   - Label background height, width-per-font-pixel and padding increased by the same 2.25x factor.
   - Vertical placement was raised to clear the smaller disc and larger label panel.

2. Map shell controls
   - World Map and Claim Map left-column controls shifted three pixels right.
   - Back moved to the bottom of each left tool column.
   - Close moved to the top-right of each complete map shell.
   - Existing bottom status bars remain available without button overlap.

Validation
----------
PASS - Targeted javac 21 compilation of MapMarkerRenderer completed with minimal Minecraft/SSU signature stubs.
PASS - Full Java lexical/string/comment and delimiter-balance scan completed for 296 source files.
PASS - All 14 JSON resources parsed successfully.
PASS - Eleven focused source assertions confirmed the dev12.7 version, halved disc, 2.25x label sizing and both map layouts.
PASS - A Gradle wrapper build attempt confirmed the documented environment limitation: Java 21 is installed and Gradle 9.2.1 cannot be downloaded because external network access is unavailable.

Packaging/build limitation
--------------------------
A full Gradle/NeoForge build is not available in the work container because the project requires Java 25,
while the container provides Java 21, and the Gradle 9.2.1 distribution is not cached for offline use.
Build locally with Java 25 using:

    gradlew.bat clean build

Runtime status
--------------
Statically validated test source. Not yet built or confirmed in Minecraft by the user.
