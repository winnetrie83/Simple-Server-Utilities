SSU 1.9.0-dev3.6.5 validation report
=====================================

Scope
-----
Targeted HUD spacing update built on SSU 1.9.0-dev3.6.4.

Implemented
-----------
- Extended MinigameCastBarPayload with a bounded instruction line.
- CTF flag-taking and Domination base-claiming now send the same dedicated instruction line.
- Removed the initial overlapping vanilla action-bar instruction for both game types.
- Kept the existing capture label and progress bar coordinates unchanged.
- Rendered the instruction centered at y - 28, sixteen logical pixels above the existing label at y - 12.
- Raised network protocol from 91 to 92.
- Raised mod version to 1.9.0-dev3.6.5.
- No storage or schema changes.

Validation performed
--------------------
- 649 Java source files passed a stateful lexical delimiter/string/comment integrity scan.
- 15 JSON files parsed successfully.
- 8/8 targeted source invariants passed:
  * version is dev3.6.5
  * protocol is 92
  * payload contains the instruction field
  * CTF sends the instruction through the custom cast payload
  * Domination sends the instruction through the custom cast payload
  * old CTF action-bar instruction is absent
  * old Domination action-bar instruction is absent
  * client renderer places the instruction above the label
- MinigameCastBarPayload and MinigameCastBarClientState compiled successfully against focused Minecraft/API stubs.
- Source ZIP integrity test passed.

Full Gradle status
------------------
A full Gradle/NeoForge build could not start in the container. Gradle 9.2.1 is not cached and services.gradle.org is unreachable (UnknownHostException). The container also provides Java 21 while SSU requires Java 25.

Required local verification
---------------------------
Run with Java 25:
  gradlew.bat clean build

Install the exact same dev3.6.5 build on client and server because the network protocol is 92.
