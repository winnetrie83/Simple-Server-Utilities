SSU 1.6.0-dev4 validation report
Date: 2026-07-30

Baseline
- Parent source: SSU 1.6.0-dev3
- Mod version: 1.6.0-dev4
- Network protocol: 26
- Target: Minecraft 26.2 / NeoForge 26.2.0.7-beta / Java 25

Static source validation
- Java files: 256
- Java lines: 45231
- JSON resources parsed successfully: 13
- Package declarations match their source paths.
- Public top-level type names match their filenames.
- No duplicate project fully-qualified class names were found.
- All project-local imports resolve to an existing top-level or nested project type.
- Java delimiter, string, character, text-block and comment balance checks passed.
- A full dependency-free javac diagnostic pass reported no syntax-like diagnostics; missing Minecraft/NeoForge/Gson/SLF4J dependency diagnostics were expected.
- The new CropsHarvestingEvents source passed an isolated stub-compilation harness, including its generic Property growth/reset helpers.
- HologramDefinition passed an executable migration harness: schema-1 scale 8 becomes schema-2 scale 1 exactly once, while a fresh schema-2 scale 8 remains scale 8.
- No obsolete LocalPlayer.displayClientMessage call remains.
- The unintended JUnit src/test tree is absent.

Targeted implementation checks (41/41)
- Version is 1.6.0-dev4 and protocol is 26.
- Hologram rendering uses the new scale-1 baseline and matching multiline spacing.
- Editor, open payload and commands consistently use scale range 1 through 8.
- Hologram storage is schema 2, migrates legacy scale once and queues migrated data for persistence.
- Non-finite hologram scale/range/image values are rejected or normalized safely.
- In-world selection uses narrow camera-facing per-line rectangles; the legacy sphere is absent and the nearest intersected plane wins.
- Treecapitator and Veinminer still require main-hand axe/pickaxe tags and the enum switch explicitly covers NONE.
- Treecapitator discovery and revalidation require the exact targeted log block/species.
- Treecapitator preview/mining uses a target-Y lower boundary and a connected breadth-first upward selection.
- Canopy validation tolerates a nearby different tree species, while automatic leaf cleanup retains conservative ownership checks.
- Every automatic non-leaf break uses the single-durability helper.
- The temporary mining-context copy is kept away from its break threshold, and the real tool receives one hurtAndBreak attempt.
- Natural leaves remain direct, durability-free destruction.
- Required-tool checks continue throughout the multi-block chain.
- Crops Harvesting is registered on the server event bus.
- Crops Harvesting has a global config toggle plus custom/disabled crop block lists.
- Progression permission ssu.crops_harvesting.use and wildcard catalog entries are present and included in default-rank migration.
- Mature-only crop detection resets the selected growth property to its first state and uses normal block loot drops.
- Vanilla, common c:crops, legacy forge:crops, conventional property and configured custom crop paths are present.
- Upper-half crop clicks resolve to the lower root and double-height reset avoids a second loot roll.
- Sweet berries and melon/pumpkin stems retain native/non-harvest behaviour.
- Crop harvesting enforces both break and place protection through ProtectionHelper.canPlayerModify.
- Dashboard snapshot encoding/decoding, server action routing and Admin Tools ON/OFF control carry the global crop state consistently.

Build limitation in this environment
- ./gradlew clean compileJava was attempted.
- The wrapper could not download Gradle 9.2.1 because outbound DNS is unavailable in this container (UnknownHostException for services.gradle.org).
- The container provides Java 21, while the project requires Java 25.
- Therefore this report does not claim a completed Minecraft/NeoForge compile or in-game test. Run gradlew.bat clean build with Java 25 on the development machine before installing the test JAR.
