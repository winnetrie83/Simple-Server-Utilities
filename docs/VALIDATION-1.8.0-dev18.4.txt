Simple Server Utilities 1.8.0-dev18.4 validation
Date: 2026-08-03

Target
- Minecraft 26.2
- NeoForge 26.2.0.7-beta
- Java 25
- Network protocol 68

Completed static validation
- 530 Java source files passed lexical delimiter/string/comment structure validation.
- A javac source pass found no syntax, malformed declaration, unclosed token or effectively-final diagnostics. Missing Minecraft/NeoForge/Gson dependencies are expected in the isolated Java 21 container.
- The five new region-selection payload records compiled against focused API stubs after their final codec layout was applied.
- 14 JSON resource/config files parsed successfully.
- Payload registrations and client handlers were verified for open, action, result, client export and client upload.
- Admin Center no longer exposes the Region Maintenance tile.
- Admin Center Regions no longer creates Rent, Extend or Unrent row buttons.
- Region Tool right-click routing reaches the new server-authoritative selection action service.
- Inventory fill entries are restricted to BlockItem on both client and server and percentages must total exactly 100.
- Selection templates serialize block states only. Block-entity data, inventories and entities are not serialized.
- Copy, paste, fill and clear operations use the SSU job scheduler and cuboid/intersecting-region resource locks.
- Existing SSU storage/schema constants are unchanged. The new .ssusel file format has its own internal version 1.
- No trailing whitespace, TODO or FIXME markers were found in the new region-selection sources.
- Source archive integrity was tested after packaging.

Build limitation
- A full Gradle/NeoForge compile could not be executed in this container. Gradle 9.2.1 is not cached and the offline wrapper attempted to download it; the container also provides Java 21 instead of the project-required Java 25.
- Run `gradlew.bat clean build` locally with Java 25 and test the checklist in TESTING-1.8.0-dev18.4.md.
