SSU 1.9.0-dev3.8 validation report
===================================

Base:
  SSU 1.9.0-dev3.7.1

Version:
  mod_version = 1.9.0-dev3.8
  network protocol = 93 (unchanged)
  persistent schemas = unchanged

Implemented TODO scope:
  1. Dynamic per-kit permission keys exposed through rank/player Permission Editor.
  2. Fixed-column minigame result table alignment.
  3. Mandatory onboarding `Decline & leave` with confirmation and no acceptance persistence.
  4. Spleef temporary projectile grants/consumption made compatible with minigame inventory lock.
  5. GUI manual minigame arena restore to saved snapshot, blocked while arena is in use/resetting.
  6. Region Setup Tool navigation/labels/workflow clarified without enlarging the compact 690x400 screen.

Static/source validation:
  Java source files scanned: 701
  Lexical delimiter/string/comment errors: 0
  JSON files parsed (excluding build/.gradle): 15
  JSON parse errors: 0
  Local SSU imports unresolved: 0
  Directed TODO/source invariants: 54/54 passed

Key invariants verified:
  - Permission Editor merges configured KitDefinition permission keys even before assignment.
  - ssu.kits.use, ssu.kits.admin and ssu.kits.* are catalogued.
  - Results headers and data share fixed anchors; winner star is independent of player-name anchor.
  - Onboarding decline action disconnects without marking rules/onboarding complete.
  - Spleef authorized grants update the inventory-lock baseline.
  - Spleef projectile consumption is accepted on the next tick before inventory lock enforcement.
  - Manual arena restore checks active match/reservation, current reset and saved snapshot before reusing scheduleArenaReset().
  - Region Tool uses task-oriented Region/Protection/Access & rent/Auto reset/Selection/Browse navigation and distinct reset-vs-portable snapshot wording.
  - Duplicate `Create region from current selection` route remains absent from Browse.

Full Gradle/NeoForge compilation in this container:
  NOT AVAILABLE.
  The Gradle wrapper attempts to download Gradle 9.2.1 from services.gradle.org, but outbound access fails with UnknownHostException.
  The container also provides OpenJDK 21.0.10 while this project requires Java 25.

Required local verification:
  gradlew.bat clean compileJava
  or
  gradlew.bat clean build

Then run the runtime checklist in TESTING-1.9.0-dev3.8.md on a dedicated server/client pair using the exact same dev3.8 build.
