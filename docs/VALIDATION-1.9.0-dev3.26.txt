SSU 1.9.0-dev3.26 validation
==============================

Baseline
- Built on SSU 1.9.0-dev3.25 source.
- Java source files in packaged tree: 794.

Version/schema checks
- mod_version = 1.9.0-dev3.26
- Network protocol = 107 (was 106)
- NPC dialogue schema = 2 (was 1)
- NPC definition schema = 10 (unchanged)
- NPC placement schema = 4 (unchanged)
- NPC Shop schema = 4 (unchanged)
- Quest persistence schemas and all other persistence schemas unchanged.

Targeted source invariants
- 30/30 targeted checks passed.
- Dialogue node ContentCondition + fallbackNode fields present.
- Player-specific fallback resolver present and used for open/current/next/continued dialogue routing.
- Server save validates node and choice condition handlers.
- Missing/self/cyclic fallback validation present.
- quest_available is registered and delegates to QuestManager.validateStart(..., "npc").
- Quest marker bridge emits priority ? > ! > bullet from real quest state/access checks.
- NPC label payload carries a bounded player-specific quest marker.
- Client quest marker rendering scales text and vertical offset with live NPC SCALE.
- Dialogue Editor 2.1 exposes node conditions, fallback selection, quest target picker and rich-text editing.
- Live dialogue and client preview use SsuRichTextComponents parsing.

Pure-Java compile/test
- Compiled ContentId, ContentDataMap, ContentAction, ContentCondition,
  NpcDialogueChoice, NpcDialogueNode, NpcDialogueDefinition and NpcDialogueValidation with javac.
- Test graph for ready -> active -> offer -> done validated successfully.
- Explicit fallback cycle was rejected.
- Result: DIALOGUE_326_PURE_TEST_OK.

Parse-oriented javac check
- Ran javac -proc:none against every dev3.26-changed Java source.
- Expected unresolved package/type diagnostics occur because Minecraft, NeoForge and Gson are not on the standalone javac classpath.
- No parse/syntax diagnostics were found (no '; expected', illegal start, unexpected EOF, unclosed token, orphaned case, etc.).

Gradle compile limitation
- Attempted: ./gradlew compileJava --offline
- Wrapper still attempted to obtain Gradle 9.2.1 from services.gradle.org.
- Environment has no external network access, ending in java.net.UnknownHostException.
- Therefore a full NeoForge/Gradle compile was not claimed here; local Java 25/Gradle testing remains required.

Runtime test focus
- See docs/TESTING-1.9.0-dev3.26.md for migration, node fallback routing,
  quest offer/turn-in, player-specific quest markers, rich text and regression checks.
