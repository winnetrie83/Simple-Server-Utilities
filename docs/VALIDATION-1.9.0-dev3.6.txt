SSU 1.9.0-dev3.6 validation report
===================================

Base
----
Built on: SSU 1.9.0-dev3.5
Target mod version: 1.9.0-dev3.6
Network protocol: 91
Player Claim storage schema: 3
Region storage schema: 5
Portable selection snapshot format: 1
Minigame definition/recovery/progression/match-history: 19/4/3/1
Player UI preferences: 11
Title catalogue / Player Identity: 1/1

Implemented scope
-----------------
- Removed duplicate point-at-current-position controls from Region Setup.
- Preserved in-world point selection through the existing Region Tool.
- Added local-region recognition plus a bounded All regions browser for remote editing.
- Added safe Teleport to region using configured spawn first and a safe region fallback otherwise.
- Integrated Create region from selection, clear-to-air, water/lava fill and weighted inventory block fills.
- Added portable full selection snapshots preserving block states, block-entity NBT/inventories,
  hanging entities and armor stands.
- Added bounded, non-destructive ghost preview with X/Y/Z movement, rotations, mirrors,
  cancel and explicit server-authoritative confirmation.
- Added client/server preview cleanup on screen close and logout.

Static validation performed
---------------------------
PASS - 648 Java source files passed lexical delimiter/string/comment integrity scanning.
PASS - All 15 JSON files parsed successfully.
PASS - 48/48 targeted source invariants passed.
PASS - RegionSetupOpenPayload and RegionSnapshotPreviewPayload compiled against focused API stubs.
PASS - RegionSetupToolService compiled against focused internal/Minecraft/NeoForge API stubs.
PASS - RegionSetupScreen, preview client state/renderer and related payload wiring were targeted
       against focused API stubs during implementation.
PASS - RegionSetupOpenPayload constructor/decode arity remained aligned after expansion.
PASS - Snapshot transform position mapping was checked for bijection across all supported transforms.
PASS - Preview packet block count is bounded to 4,096 while confirmed placement uses the full snapshot.
PASS - Portable snapshot files use bounded gzip decoding, atomic replacement and a separate snapshots folder.
PASS - Final source no longer contains the duplicate point-selection button labels.
PASS - Preview confirmation rechecks region-edit permission server-side.
PASS - Service-level local-variable/dispatch compilation check passed after final cleanup.

Full build status
-----------------
A full Gradle/NeoForge compilation could not be started in this container.
The Gradle wrapper attempted to download Gradle 9.2.1 from services.gradle.org, but network/DNS
access is unavailable (UnknownHostException). The container also provides Java 21, while SSU
requires Java 25.

Therefore this source is statically and selectively compiled/validated, but it is not claimed as
fully Gradle-built or ingame-confirmed. Build locally with Java 25 using:

    gradlew.bat clean build

Then test with exactly dev3.6 on both client and dedicated server.
