Simple Server Utilities 1.4.0-dev4 validation report
Date: 2026-07-29
Source baseline: SSU-1.4.0-dev3-source.zip
Target: Minecraft 26.2 / NeoForge 26.2.0.7-beta / Java 25
Network protocol: 19

RESULT SUMMARY
- 49/49 structural and integration assertions passed.
- 191 Java source files scanned.
- git diff --check passed (no whitespace/error markers).
- Full-source javac parse scan found no Java syntax diagnostics such as missing semicolons,
  illegal starts, duplicate method parameters, unexpected lambdas or ambiguous requestTeleport overloads.
  External Minecraft/NeoForge/Gson classes are not present in this container, so this was not a
  dependency-complete compilation.
- TeleportMovementGuard/TeleportManager harness passed.
- TeleportPolicy harness passed.
- PermissionCatalog harness passed.
- No src/test or JUnit source set is present.

VALIDATED DEV4 BEHAVIOUR
- New canonical permission keys exist and are catalogued:
  ssu.teleport.escape
  ssu.teleport.region_bypass
  ssu.teleport.require_still
- Legacy ssu.teleport.cancel_on_move remains a fallback for existing data.
- Legacy ssu.spawn.region_bypass remains supported for spawn-only region denies.
- Homes, warps, spawn, player claims and accessible regions use the shared TeleportManager.
- Normal delayed teleports re-check current permission context before execution.
- Execution-time denial messages are generated from the player's current region/dimension.
- Explicit effective-region teleport denies remain authoritative unless the appropriate bypass applies.
- Movement is compared using exact 3D coordinates and dimension, with a small correction tolerance.
- Walking inside one block, jumping/falling and dimension changes cancel when require_still=true.
- Movement does not cancel when require_still=false.
- Cancelled or denied pending teleports do not receive cooldown.
- Administrative claim teleports remain immediate and separate from player escape policy.
- Dimension permission scopes are exposed through Admin Center -> Permissions.
- Dimension GUI and commands use the shared PermissionCatalog validation and persistence path.
- No direct player.teleportTo call remains outside TeleportManager.
- Existing storage schemas remain unchanged; no mandatory data migration was introduced.

HARNESS DETAILS
1. TeleportManagerHarness
   PASS: movement cancellation with require_still=true
   PASS: movement allowed with require_still=false/immediate path
   PASS: execution guard is checked again at execution time
   PASS: dynamic execution-time failure message
   PASS: legacy String failure-message overload remains usable

2. TeleportPolicyHarness
   PASS: region spawn deny overrides ordinary allow
   PASS: ssu.teleport.region_bypass works
   PASS: legacy ssu.spawn.region_bypass works for spawn
   PASS: ssu.teleport.escape region deny blocks all normal teleport types
   PASS: legacy cancel_on_move=false is used when the new key is absent
   PASS: require_still explicitly configured takes precedence
   PASS: dimension denial message identifies the dimension

3. PermissionCatalogHarness
   PASS: new keys are registered
   PASS: require_still is boolean typed
   PASS: allow normalizes to true
   PASS: deny normalizes to false

BUILD LIMITATION
The complete Gradle/NeoForge build could not be executed in this container because:
- the container runtime is Java 21 rather than the required Java 25;
- Gradle 9.2.1 is not cached;
- the environment cannot download the Gradle distribution from services.gradle.org.

REQUIRED LOCAL VALIDATION
Run on Windows with Java 25:
  gradlew.bat clean build

Then install the resulting 1.4.0-dev4 JAR on both client and server and execute:
  docs/TESTING-1.4.0-dev4.md

STATUS
Static/source/harness validation: PASSED
Full NeoForge compile: NOT RUN IN CONTAINER
Minecraft runtime/UI validation: REQUIRED LOCALLY
