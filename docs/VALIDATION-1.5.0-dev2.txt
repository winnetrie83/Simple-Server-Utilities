SSU 1.5.0-dev2 source validation
================================
Date: 2026-07-30
Base: user-confirmed working SSU 1.5.0-dev1.1
Target: Minecraft 26.2 / NeoForge 26.2.0.7-beta / Java 25
Network protocol: 21

SOURCE INVENTORY
----------------
PASS - 215 Java source files.
PASS - approximately 39,568 Java source lines.
PASS - no build, .gradle, .git, class, log or temporary output was included in the source tree.

STRUCTURAL AND INTEGRATION VALIDATION
-------------------------------------
PASS - 111/111 focused source/integration assertions.
PASS - version and protocol wiring are consistent.
PASS - mailbox schema 3 and player UI preference schema 2 migrations are present.
PASS - sent-mail deletion, double-confirmed bulk clearing and permission cap are wired client/server.
PASS - bulk Inbox clear preserves unread mail, queued overflow and unclaimed attachments.
PASS - outgoing anti-spam history is independent from the removable Sent Mail list.
PASS - opened/item-claimed/money-claimed timestamps are stored, encoded and displayed.
PASS - suggestion request/response packets are registered in the correct directions.
PASS - known-player suggestions use permission, mailbox and online-player indexes.
PASS - typed recipients are validated before attachment serialization or money escrow.
PASS - invalid recipients keep the compose container and its fields/attachments intact.
PASS - locked attachment slots render a placeholder lock and remain server-authoritatively inactive.
PASS - inventory-key handling is isolated inside the composer and forwards the key to focused text boxes.
PASS - all three attachment-mail auto-delete preferences default to OFF and map to PLAYER, SYSTEM/RECOVERY and AUCTION sources.
PASS - auto-delete requires all item and money attachments to be claimed and never targets text-only mail.
PASS - current personal preferences are categorized under General, Minimap, World map, Borders and Mail.
PASS - world-map overlay preferences persist through the server-backed player UI settings store.
PASS - personally selected region borders can be cleared from the Borders settings category.
PASS - all Java package declarations match their source paths.
PASS - lexical delimiter scan over all Java files found no unbalanced source delimiters.

TARGETED HARNESSES
------------------
PASS - pure Java mail model harness:
       - sender open/item/money status transitions;
       - schema-2 sent-limit migration to 20;
       - explicit schema-3 sent limit 0 preservation;
       - clearing Sent Mail does not clear rolling send history;
       - old sent timestamps seed anti-spam history;
       - durable delivery receipts survive mail deletion.
PASS - pure Java player-settings harness:
       - all new mail auto-delete defaults are OFF;
       - PLAYER, SYSTEM/RECOVERY and AUCTION source mapping;
       - old world-map behavior migrates with overlays enabled.
PASS - isolated network payload stub compilation for:
       - MailDataPayload;
       - MailRecipientSuggestionsRequestPayload;
       - MailRecipientSuggestionsPayload;
       - PlayerUiSettingUpdatePayload;
       - SsuMenuSnapshotPayload;
       - WorldMapDataPayload.
PASS - javac source parser scan found no syntax/parser markers. Full type resolution was intentionally unavailable without Minecraft/NeoForge libraries.

FULL GRADLE BUILD
-----------------
NOT RUN IN THIS CONTAINER.
The included wrapper attempted to download Gradle 9.2.1, but this environment has no external network access (UnknownHostException for services.gradle.org). The container also runs Java 21 instead of the required Java 25.

Required local validation:
    gradlew.bat clean build

Then install the generated build/libs JAR on both client and server and execute docs/TESTING-1.5.0-dev2.md.

RUNTIME STATUS
--------------
SSU 1.5.0-dev2 has not yet been built or tested in Minecraft by the user.
