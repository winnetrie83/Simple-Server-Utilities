SSU 1.4.0-dev1.3 validation report
==================================

Scope
-----
- Compact Players & Permissions layout
- Permission tooltips with explicit default/effective values
- Administrator Player Info & Profile browser
- Online/offline alphabetical player selection and filtering
- Paged effective-permission profile list
- Direct Player Profile -> Edit permissions handoff
- Network protocol 15

Executable checks completed
---------------------------
PASS  PermissionCatalog, PermissionKeys and PermissionValueResolver compiled and executed in a pure Java harness.
PASS  All 60 PermissionKeys constants resolve to non-empty metadata; the full catalogue contains 74 entries including wildcards.
PASS  Exact permission values take precedence over wildcard values and custom keys remain included.
PASS  New player-profile request/data payloads and the updated permission-editor payload compiled against a focused Minecraft network API stub.
PASS  Payload constructor harness verified player/query trimming, page/request bounds and oversized permission-list rejection.
PASS  In-memory codec round trips preserved the complete player profile and the new permission defaultValue field.
PASS  SsuDashboardScreen compiled with the updated permission layout, Player Info page and both new payloads against focused client API stubs.
PASS  SsuMenuService compiled with profile assembly, permission resolution and offline economy-account merging against focused server API stubs.
PASS  The permission page uses dynamic page sizing capped at 10 compact rows and keeps the selected-target summary below the search controls.
PASS  Permission row rendering no longer emits the repeated module-default/effective subtitle; the tooltip includes Default, effective source and direct override.
PASS  The Player Info dropdown is server-sorted case-insensitively and merges permission-known, economy-known and currently online players.
PASS  Profile data requests are administrator-gated server-side; Edit permissions is shown only with permission-admin access.
PASS  Network registration uses protocol 15 and registers both profile request/response payloads.
PASS  Client payload dispatch routes profile data only to an open SsuDashboardScreen and stale request IDs are rejected.
PASS  All 13 JSON resources parse successfully.
PASS  All 13 PNG resources have valid signatures, chunks, IEND markers and CRCs.
PASS  Lightweight Java lexical balance scan passed for all 180 Java source/test files.
PASS  No invalid direct ServerPlayer#getServer() calls remain in main Java sources.
PASS  git diff --check reports no whitespace errors.

Full Gradle/NeoForge build status
---------------------------------
NOT RUN TO COMPLETION in this environment.
The included wrapper attempted to download Gradle 9.2.1 and failed with:
java.net.UnknownHostException: services.gradle.org

The available runtime is Java 21 while SSU targets Java 25. The final build and Minecraft runtime test must be performed locally with Java 25:

gradlew.bat clean test build

Runtime checks still required
-----------------------------
- Confirm the selected player/rank summary no longer overlaps the permission-search field at all GUI scales.
- Verify ten compact permission rows fit where space permits and smaller screens reduce the page size safely.
- Hover boolean, integer, wildcard and custom permissions and compare Default/effective/direct values.
- Verify alphabetical online/offline player dropdown contents and partial/exact-name searches.
- Compare profile balance, claims, chunks, homes and rentals against existing commands/pages.
- Verify online-only dimension, position, health and food data and safe offline placeholders.
- Page through all effective permissions and compare values/sources with the permission editor.
- Use Edit permissions and confirm the correct player is already selected; Back must return to the same profile.
- Confirm profile inspection is denied to non-admin packets and editing is unavailable without ssu.permissions.admin.
- Restart the server and confirm no existing save data changed.
- Confirm protocol-14 and protocol-15 installations are not mixed.
