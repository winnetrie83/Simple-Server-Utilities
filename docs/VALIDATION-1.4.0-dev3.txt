SSU 1.4.0-dev3 validation report
================================

Scope
-----
Base: SSU 1.4.0-dev2.1
Target: SSU 1.4.0-dev3
Minecraft: 26.2
NeoForge: 26.2.0.7-beta
Java target: 25
Network protocol: 18

Implemented scope
-----------------
- Added a persistent dimension-aware server spawn and a Core 2.0 Spawn module.
- Added /spawn, /spawn set|clear|info|cancel, /setspawn and /delspawn.
- Added spawn use/admin/delay/cooldown/region-bypass permissions and default-rank migration.
- Added Server Spawn and administrator set/clear controls to the dashboard Travel page.
- Added a searchable, paged, server-authoritative region-permission editor with Default/Allow/Deny and typed Set/Reset controls.
- Unified GUI and /regions perm mutations through PermissionCatalog validation, normalized region keys, persistence and cache invalidation.
- Made an effective-region ssu.spawn.use deny authoritative over ordinary personal/rank allows unless the player has ssu.spawn.region_bypass.
- Added an execution guard to delayed teleports and re-check /spawn region access immediately before execution.
- Kept all existing data schemas compatible; the optional spawn file is created only after an administrator sets a spawn.

Checks performed
----------------
PASS: git diff --check reports no whitespace or patch-format errors.
PASS: 190 Java source files passed a lexical delimiter/string/comment scanner.
PASS: all SSU package paths and local imports resolve to source files.
PASS: all 2 resource JSON files parse successfully.
PASS: all 13 PNG resources have valid PNG signatures, chunk structures and CRC values.
PASS: no files exist under src/test.
PASS: no JUnit, unitTest, testImplementation or testRuntimeOnly configuration remains.
PASS: version metadata is 1.4.0-dev3 and the payload registrar protocol is 18.
PASS: all seven new dev3 Java source files are present.
PASS: all 65 PermissionKeys references resolve to declared constants.
PASS: Spawn module registration, reload integration and isolated storage path are present.
PASS: spawn permissions are present in PermissionKeys, PermissionCatalog and default-rank migration.
PASS: /spawn and both aliases are registered.
PASS: dashboard Travel actions and the snapshot spawn-administration flag are wired.
PASS: region permission navigation, request mode, data page and set/unset actions are wired client/server.
PASS: region commands normalize values through PermissionCatalog and invalidate the permission cache.
PASS: strict region deny, bypass-without-region and delayed execution-guard anchors are present.
PASS: javac parsing of the complete source tree reported no syntax-like diagnostics; unresolved Minecraft/NeoForge dependencies prevent a full type check in this isolated runtime.
PASS: ServerSpawn and SpawnPolicy compiled and passed a focused behaviour harness, including deny, bypass, wildcard-deny and non-region-deny cases.
PASS: TeleportManager compiled and passed a focused execution-guard harness, including initial denial, delayed re-check and no execution after denial.
PASS: ServerSpawnManager compiled and passed a focused set/clear storage-owner harness.
PASS: RegionPermissionScreen compiled in a focused Minecraft client-API harness.

Full Gradle/NeoForge build status
---------------------------------
NOT RUN successfully in this container. The included wrapper attempted to download
Gradle 9.2.1 from services.gradle.org, but the isolated command environment has no
DNS/network access (UnknownHostException). The installed runtime is Java 21 while
this project deliberately targets Java 25.

Required local verification
---------------------------
Run on the development machine with Java 25:

    gradlew.bat clean build

Then install the generated JAR from build/libs on both client and server and test
with a copy of the world. Follow docs/TESTING-1.4.0-dev3.md, especially persistent
spawn, GUI/command region overrides, strict deny versus bypass, delayed region
re-checks, overlapping-region priority, restart persistence and regressions.
