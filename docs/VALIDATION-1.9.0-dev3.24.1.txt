Simple Server Utilities 1.9.0-dev3.24.1 — static validation

Base: 1.9.0-dev3.24
Target: 1.9.0-dev3.24.1
Network protocol: 105 (unchanged)
NPC schema: 9 (unchanged)

NPC label checks
- NpcManager runtime apply path uses setCustomName(null) and setCustomNameVisible(false).
- No other NPC runtime setCustomName call remains.
- NpcLabelRenderer base name scale: 0.40 (was 0.20).
- NpcLabelRenderer small/role/faction scale: 0.29 (was 0.145).
- Live client Attributes.SCALE is applied to text size and label spacing.

Remote texture checks
- HTTPS-only source validation remains in place.
- 512 KiB hard download cap remains in place.
- 64x64 PNG validation remains in place.
- URL requests now send image/browser-compatible User-Agent, Accept and same-origin Referer headers.
- Failed downloads are retried only after a bounded 30-second cooldown.
- Reported minecraftskins.com test asset was externally verified as a direct 64x64 PNG below the size cap.

Syntax-oriented javac pass
- Modified sources were parsed by javac; diagnostics were dependency-resolution errors only because the container lacks the Minecraft/NeoForge classpath.
- No Java syntax diagnostics were observed in the modified source.

Full Gradle/NeoForge compilation is still required locally with Java 25 and the project toolchain.
